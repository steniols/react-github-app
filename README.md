# React github client

## API

```bash
cd api
cp .env.sample .env # and check/update configs
npm install
npm start
```

## Client

```bash
cd client
cp .env.sample .env # and check/update configs
npm install
npm start
```